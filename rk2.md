# Задача 1
![1](assets/1.png)

Это язык 
$$L = \{(ab^s_1ab^{s_1+1})(ab^s_2ab^{s_2+1})\cdots(ab^s_nab^{s_n+1}), s_i \geq 0, n \geq 0, |w_a| \neq |w_b| \}$$
# Задача 2
![2](assets/2.png)


# Задача 3
![3](assets/3.png)

Заметим, что атрибут A хранит число букв b, им порожденных. 

Правила 

$S \to AaaSaaA; \ A_2.attr < A_1.attr $

$S \to AbSA; \ A_2.attr > A_1.attr $

Стремятся к тому, чтобы выровнять количество b слева и справа от символа S. Когда балланс найден, S терминирует в b.

В итоге получаются слова, в которых вокруг центральной b одинаковое число b.

Для накачки возьмем
$$w = b^nab^n b \bold{b}b a b^nab^n = uvxyz,  |vxy| < n$$


Назовем b, в которую терминировала S терминальной.

Случаи:
1) **vxy полностью в одном из блоков $b^n$.** 

Если это первый блок, тогда, например при длине накачки, обеспечивающей вид нового слова:
$$w' = b^{n + k}ab^n b \bold{b}b a b^nab^n = uvxyz, n + k > 5n$$
И тогда, чтобы выровнять баланс букв b, терминальная b должна быть как раз в блоке $b^{n + k}$, но это невозможно, потому что блок слева от нее должен иметь хотя бы одну a (в которую терминировала A).


Если это второй блок, то опять же, при достаточно накачке терминальная b переедет во второй блок:
$$w' = b^{n}ab^{m}\bold{b}b^s b bb a b^nab^n = uvxyz, n + k > 5n$$
Левая часть должна иметь $m = n$, но тогда баланса букв b не будет.

Аналогично для других блоков в силу симметрии.

Дальше для простоты $$w = b^nab^{n+3} a b^nab^n = uvxyz$$

2) **vxy на пересечении $b^nab^{n+3}$.**

Если $vxy$ содержит букву a, то при отрицательной накачке буква a убирается и убирается меньше n букв b.
$$b^kab^nab^n, 2n + 3 \geq k > n$$
Терминальная b не в блоке $b^k$, т.к. там должна быть хотя бы одна a (терминальная A) и не в последнем, так как не будет баланса. Тогда она во втором блоке, и так как и в левой и в правой части только одна буква a, то это $A \to bAb$, поэтому это слово типа $$b^kab^kbb^nab^n$$, но это невозможно, так как k + n + 1 > n.


Если $vxy$ не содержит a, то получим






